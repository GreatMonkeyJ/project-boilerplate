extends components/layouts/layout

block append contents
  section#home
    div.inner
      strong.title HyunJiwon
      div.content
        span Front-End Developer
      i.scrollDown ScrollDown

  section#experience
    div.inner
      strong.title Experience
      div.content
        - var experienceData = require('./src/js/data/experience.js');
        ul.expList
          each experience in experienceData
            li
              div.date
                span.start= experience.date.start
                span.end= experience.date.end
              div.company
                strong.employer= experience.company.employer
                span.position= experience.company.position
                div.details !{experience.company.detail}

  section#skills
    div.inner
      strong.title Skills
      div.content
        - var skills = require('./src/js/data/skills.js');
        ul.skillList
          each skill in skills
            li(style=`border-color:${skill.pointColor}; --percent:${skill.proficiency};`)
              div(style=`background-color:${skill.pointColor};` class="info")
                strong.name= skill.name
                span.proficiency= skill.proficiency
              div.wave

  section#portfolio
    div.inner
      strong.title Portfolio
      div.content
        - var portfolio = require('./src/js/data/portfolio.js');
        ul.portfolioList
          each project in portfolio
            - console.log(project)
            li
              div.info
                div.logo
                  strong.title= project.title
                    if !!project.subTitle
                      span= project.subTitle
                div.detail
                  p.desc= project.description
                  if project.protected
                    button(type="button" class="view" disabled="disabled") Protected
                  if !!project.href
                    a(href=`${project.href}` target="_blank" class="view") View

  section#contact
    div.inner
      strong.title Contact
      div.content
        form(action="https://formspree.io/xayynlqk" method="POST" id="contactForm")
          input(type="text" name="name" id="name" placeholder="이름" required)
          input(type="email" name="email" id="email" placeholder="이메일" required)
          textarea(name="message" id="message" placeholder="메시지를 입력해주세요." required)
          button(type="submit" id="contactFormSubmit") 전송

block append body
  <!--[if !IE]> -->
    script(src="//cdnjs.cloudflare.com/ajax/libs/gsap/2.1.3/TweenMax.min.js")
    script(src="//cdnjs.cloudflare.com/ajax/libs/ScrollMagic/2.0.7/ScrollMagic.min.js")
    script(src="//cdnjs.cloudflare.com/ajax/libs/ScrollMagic/2.0.7/plugins/animation.gsap.js")
    script(src="//cdnjs.cloudflare.com/ajax/libs/ScrollMagic/2.0.7/plugins/debug.addIndicators.min.js")
    script(src="/js/ui/index.js")
    script(src="/js/ui/animation.js")
  <!--<![endif]-->
